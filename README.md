# homebrew-tap

Homebrew tap for [SafeCut](https://github.com/Rafaelhdsg/safecut).

```bash
brew tap Rafaelhdsg/tap
brew install --cask safecut
```

Legacy Formula installs are migrated automatically via `tap_migrations.json`.
